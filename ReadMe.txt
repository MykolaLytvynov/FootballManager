Before starting, you need to create a Database, and change the database settings in the application.properties.

In the created database, add new tables and data to them, all the necessary queries are in the files createTables.sql and requestsToStart.sql in the application.properties.

Now you can run the application